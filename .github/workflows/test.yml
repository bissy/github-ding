name: Test CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Run simple test
      run: |
        echo "Testing GitHub PR Meow Notifier..."
        echo "All checks passed! 🎉"

    - name: Wait a bit (for demo)
      run: sleep 5

    - name: Success
      run: echo "✅ Ready to merge!"
